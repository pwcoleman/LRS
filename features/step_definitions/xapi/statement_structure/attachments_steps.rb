# encoding: UTF-8
Given(/^an \[attachment\] saveStatement request$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^an \[attachmentJSON\] saveStatement request$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^an \[attachmentFileUrlOnly\] saveStatement request$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^an attachment saveStatement request$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[description\] is changed to \[languageMap content\]$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[fileUrl\] is changed to \['http:\/\/tincanapi\.com\/conformancetest\/fileUrl'\]$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[usageType\] is changed to '\[bad type\]'$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[contentType\] is changed to '\[bad type\]'$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[display\] is changed to '\[not a language map\]'$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[description\] is changed to '\[not a language map\]'$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[length\] is changed to '\[not an integer\]'$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[sha(\d+)\] is changed to '\[not a hash\]'$/) do |arg1, arg2, arg3|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[fileUrl\] is changed to '\[bad IRL\]'$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[usageType\] is removed$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[display\] is removed$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[contentType\] is removed$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[length\] is removed$/) do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment (\d+) body attachments (\d+) \[sha(\d+)\] is removed$/) do |arg1, arg2, arg3|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment \[(\d+)\] \[Content\-Type\] is changed to '\[bad type\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment \[(\d+)\] \[body\] is changed to '\[not JSON\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment \[(\d+)\] \[body\] is changed to '\[\{"(.*?)": "(.*?)", "(.*?)": "(.*?)"\}\]'$/) do |arg1, arg2, arg3, arg4, arg5|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment \[(\d+)\] \[X\-Experience\-API\-Hash\] is changed to '\[not a matching hash\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment \[(\d+)\] \[Content\-Type\] is removed$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the attachment \[(\d+)\] \[X\-Experience\-API\-Hash\] is removed$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end
