# encoding: UTF-8
Given(/^the statement authority is changed to a \[consumerOnly authority\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[consumerAndUser authority\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[mboxAndType agent\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[mboxSha(\d+)AndType agent\]$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[openidAndType agent\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[accountAndType agent\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[mboxOnly agent\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[mboxSha(\d+)Only agent\]$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[openidOnly agent\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[accountOnly agent\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[mbox\] is changed to '\[conformancetest@tincanapi\.com\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[mbox\] is changed to '\[bad mbox\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[objectType\] is changed to '\[notAgent\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[mboxAndType group\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[objectType\] is changed to '\[notGroup\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[allPropertiesMboxAgentMember group\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[member (\d+) mbox\] is changed to '\[conformancetest@tincanapi\.com\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[member (\d+) mbox\] is changed to '\[bad mbox\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[openid\] is changed to '\[bad openid\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[accountAndType group\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[account homePage\] is changed to '\[bad homePage\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[objectType\] is changed to '\[agent\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[objectType\] is changed to '\[group\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[openidAndType group\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[allPropertiesOpenidAgentMember group\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[member (\d+) openid\] is changed to '\[bad openid\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[allPropertiesAccountAgentMember group\]$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[member (\d+) account homePage\] is changed to '\[bad homePage\]'$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[account name\] is removed$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[account homePage\] is removed$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[objectType\] is removed$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[mbox\] is removed$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[mbox_sha(\d+)sum\] is removed$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[openid\] is removed$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority \[account\] is removed$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement authority is changed to a \[mboxSha(\d+)AndType group\]$/) do |arg1|
  pending # express the regexp above with the code you wish you had
end
