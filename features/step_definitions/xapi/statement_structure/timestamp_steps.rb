# encoding: UTF-8
Given(/^the statement timestamp is changed to '\[(\d+)\-(\d+)\-(\d+)T(\d+):(\d+):(\d+)Z\]'$/) do |arg1, arg2, arg3, arg4, arg5, arg6|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement timestamp is changed to '\[(\d+)\-(\d+)\-(\d+)T(\d+):(\d+):(\d+)\+(\d+)\]'$/) do |arg1, arg2, arg3, arg4, arg5, arg6, arg7|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement timestamp is changed to '\[(\d+)\-(\d+)\-(\d+)T(\d+):(\d+):(\d+)\-(\d+)\]'$/) do |arg1, arg2, arg3, arg4, arg5, arg6, arg7|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement timestamp is changed to '\[(\d+)\-(\d+)\-(\d+)T(\d+):(\d+):(\d+)\-(\d+):(\d+)\]'$/) do |arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement timestamp is changed to '\[(\d+)\-(\d+)\-(\d+)T(\d+):(\d+):(\d+)\.(\d+)\-(\d+):(\d+)\]'$/) do |arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9|
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement timestamp is changed to '\[bad timestamp\]'$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^the statement timestamp is removed$/) do
  pending # express the regexp above with the code you wish you had
end

